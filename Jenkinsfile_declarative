pipeline {
    agent {
         dockerfile {
            filename 'Dockerfile'
            dir '.'
            args "--privileged" 
            label 'zip-job-docker'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
                sh 'svn --version'
            }
        }
    }
}
