pipeline {
    agent {
         dockerfile true
    }
    stages {
        stage('Test') {
            steps {
                echo 'Running inside a Docker container built from the Dockerfile'
            }
        }
    }
}
